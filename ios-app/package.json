{
  "name": "goatlab-ios",
  "version": "1.0.0",
  "private": true,
  "description": "GoatLab iOS shell (Capacitor). See PLAN.md - build:www bundles the site from the repo root.",
  "scripts": {
    "build:www": "node build-www.js",
    "sync": "node build-www.js && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/browser": "^6.0.2",
    "@capacitor/haptics": "^6.0.1",
    "@capacitor/splash-screen": "^6.0.2",
    "@capacitor/status-bar": "^6.0.1",
    "@capacitor-community/apple-sign-in": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
